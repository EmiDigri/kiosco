name: Sync pagos MP

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar al cron
        run: |
          curl -s https://kiosco-lac.vercel.app/api/cron
